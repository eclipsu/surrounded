{
  "name": "circle",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"wait-on http://127.0.0.1:3001/api/health -t 30000 && npm run dev -w client\"",
    "dev:lan": "node scripts/dev-lan.mjs",
    "gen-certs": "node scripts/gen-certs.mjs",
    "gen-livekit": "node scripts/gen-livekit-config.mjs",
    "livekit": "node scripts/start-livekit.mjs",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.1"
  }
}
