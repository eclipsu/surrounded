services:
  livekit:
    image: livekit/livekit-server:latest
    network_mode: host
    volumes:
      - ./livekit.yaml:/livekit.yaml:ro
    # Signaling proxied via Vite (:5173/rtc). Media is direct UDP to rtc.node_ip in livekit.yaml.
    command: --dev --config /livekit.yaml
